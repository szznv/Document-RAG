# Document-RAG

1. **LangG.py** — полнофункциональный пайплайн на LangChain+LangGraph+Docling+Qdrant+Ollama 

Оба работают на **CPU**, совместно используют **одно виртуальное окружение** и подходят для сравнения подходов RAG.

- **Важно**: Путь к проекту должен быть **только латиницей**, например `E:\RAG\`.

## 📦 Установка и первоначальная настройка
## Установка Ollama (обязательно)
Скачайте и установите Ollama для Windows .

### 1) Клонирование проекта

### 2) Создание и активация виртуального окружения
версия python 3.9!

**PowerShell:**
```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -U pip
```

**Потом остальные пакеты из requirements.txt:**
```bash
pip install -r requirements.txt
```

### 3) Настройка кэша (опционально, но рекомендуется)

Чтобы HuggingFace и Ollama не забивали диск C:, переместите кэш в папку проекта.

**PowerShell:**
```powershell
# Добавьте в переменные окружения Windows
[Environment]::SetEnvironmentVariable("HF_HOME", "E:\RAG\hf_cache", "User")
[Environment]::SetEnvironmentVariable("OLLAMA_MODELS", "E:\RAG\ollama_models", "User")
```

**Или в начало скрипта (если не хотите менять систему):**
```python
import os
os.environ["HF_HOME"] = r"E:\RAG\hf_cache"
os.environ["OLLAMA_MODELS"] = r"E:\RAG\ollama_models"
```

Затем перезапустите Ollama, чтобы переменные применились.

### 4) Скачивание моделей Ollama

```bash
# Проверить установленные модели
ollama list

# Если qwen2.5:7b не скачана
ollama pull qwen2.5:7b
ollama pull bge-m3
```

---

## 📂 Структура проекта

```
E:\RAG\
├── .venv/                         # Виртуальное окружение
│
├── doc/                           # Папка для входящих документов
│   ├── document1.pdf
│   ├── document2.docx
│   └── document3.txt
│
├── LangG.py                       # Реализация LangChain+Qdrant
├── LLmWare.py                     # Реализация llmware
│
├── requirements.txt               # Зависимости
├── qdrant_data/                   # Локальная Qdrant база (LangG.py)
│   ├── ...
│   └── sqlite_db.sqlite
│
├── hf_cache/                      # Кэш HuggingFace (если переместили)
└── ollama_models/                 # Модели Ollama (если переместили)
```

---

## ▶️ Запуск LangG.py

### Основное назначение
- **LangChain** + **LangGraph** для управления потоком RAG
- **Docling** для парсинга PDF/DOCX в Markdown (хорошо сохраняет таблицы)
- **Qdrant** для локального хранения векторов (on-disk база)
- **Ollama** для генерации ответов
- Максимальный контроль над каждым этапом (парсинг → чанкинг → эмбеддинги → retrieval → генерация)

### Запуск
```bash
python LangG.py
```

## ▶️ Запуск QUI
```bash
pip install streamlit
streamlit run app.py
```

### Кастомизация
В файле LangG.py можно менять:
- `chunk_size=1400` (размер текстовых блоков)
- `k=5` (количество найденных фрагментов)
- `embeddings` модель (по умолчанию `bge-m3` через Ollama)

---

### Если папка `doc/` пустая
- Индекс и кэш **автоматически удаляются** (clean slate)
- При добавлении файлов индексация начнётся с нуля


## 🏗️ Архитектура данных

### LangG.py (LangChain + Qdrant)
```
doc/ (входящие PDF/DOCX)
    ↓
Docling (парсинг → Markdown)
    ↓
MarkdownHeaderTextSplitter + RecursiveCharacterTextSplitter (чанкинг)
    ↓
OllamaEmbeddings (bge-m3, 1024 dims) → создание векторов
    ↓
QdrantVectorStore (on-disk, qdrant_data/) → хранение
    ↓
Query (semantic search) → поиск релевантных чанков
    ↓
ChatOllama (qwen2.5:7b) → генерация ответа
```
