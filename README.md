# Document-RAG

1. **LangG.py** — полнофункциональный пайплайн на LangChain+LangGraph+Docling+Qdrant+Ollama (максимальный контроль)
2. **LLmWare.py** — упрощённый вариант на llmware+ChromaDB+Ollama (быстрое прототипирование)

Оба работают на **CPU**, совместно используют **одно виртуальное окружение** и подходят для сравнения подходов RAG.

- **Важно**: Путь к проекту должен быть **только латиницей**, например `E:\RAG\` (не `OneDrive\Рабочий стол\...`).

## 📦 Установка и первоначальная настройка
## Установка Ollama (обязательно)
Скачайте и установите Ollama для Windows .

### 1) Клонирование проекта

### 2) Создание и активация виртуального окружения

**PowerShell:**
```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -U pip
```

### 3) Установка зависимостей

**Сначала PyTorch на CPU** (отдельно, чтобы избежать CUDA-зависимостей):
```bash
pip install torch --index-url https://download.pytorch.org/whl/cpu
```

**Потом остальные пакеты из requirements.txt:**
```bash
pip install -r requirements.txt
```

### 4) Настройка кэша (опционально, но рекомендуется)

Чтобы HuggingFace и Ollama не забивали диск C:, переместите кэш в папку проекта.

**PowerShell:**
```powershell
# Добавьте в переменные окружения Windows
[Environment]::SetEnvironmentVariable("HF_HOME", "E:\RAG\hf_cache", "User")
[Environment]::SetEnvironmentVariable("OLLAMA_MODELS", "E:\RAG\ollama_models", "User")
```

**Или в начало скрипта (если не хотите менять систему):**
```python
import os
os.environ["HF_HOME"] = r"E:\RAG\hf_cache"
os.environ["OLLAMA_MODELS"] = r"E:\RAG\ollama_models"
```

Затем перезапустите Ollama, чтобы переменные применились.

### 5) Скачивание моделей Ollama

```bash
# Проверить установленные модели
ollama list

# Если qwen2.5:7b не скачана
ollama pull qwen2.5:7b
ollama pull bge-m3
```

---

## 📂 Структура проекта

```
E:\RAG\
├── .venv/                         # Виртуальное окружение
│
├── doc/                           # Папка для входящих документов
│   ├── document1.pdf
│   ├── document2.docx
│   └── document3.txt
│
├── LangG.py                       # Реализация LangChain+Qdrant
├── LLmWare.py                     # Реализация llmware
│
├── requirements.txt               # Зависимости
├── README.md                      # Этот файл
│
├── ingest_state.json              # Кэш состояния для LangG.py
├── ingest_state_llmware.json      # Кэш состояния для LLmWare.py
│
├── qdrant_data/                   # Локальная Qdrant база (LangG.py)
│   ├── ...
│   └── sqlite_db.sqlite
│
├── llmware_data/                  # Данные llmware (LLmWare.py)
│   ├── accounts/
│   ├── library_catalog/
│   └── sqlite_llmware.db
│
├── hf_cache/                      # Кэш HuggingFace (если переместили)
└── ollama_models/                 # Модели Ollama (если переместили)
```

---

## ▶️ Запуск LangG.py

### Основное назначение
- **LangChain** + **LangGraph** для управления потоком RAG
- **Docling** для парсинга PDF/DOCX в Markdown (хорошо сохраняет таблицы)
- **Qdrant** для локального хранения векторов (on-disk база)
- **Ollama** для генерации ответов
- Максимальный контроль над каждым этапом (парсинг → чанкинг → эмбеддинги → retrieval → генерация)

### Запуск
```bash
python LangG.py
```

### Кастомизация
В файле LangG.py можно менять:
- `chunk_size=1400` (размер текстовых блоков)
- `k=5` (количество найденных фрагментов)
- `embeddings` модель (по умолчанию `bge-m3` через Ollama)

---

## ▶️ Запуск LLmWare.py

### Основное назначение
- **llmware** для упрощённого RAG (всё "из коробки")
- **ChromaDB** как векторное хранилище (проще Qdrant, но менее гибко)
- **sentence-transformers** для эмбеддингов на CPU
- **Ollama** для генерации ответов
- Быстрое прототипирование с минимумом настроек

### Запуск
```bash
python LLmWare.py
```

### Кастомизация
В файле LLmWare.py можно менять:
- `EMBEDDING_MODEL = "mini-lm-sbert"` (встроенная модель llmware)
  - Альтернатива (multilingual): `"sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2"` (!! были проблемы с реализацией)
- `batch_size=50` (размер батча при векторизации на CPU)
- `result_count=5` (количество результатов поиска)

---

Оба подхода поддерживают **русский и казахский** текст:

- **LangG.py**: использует `bge-m3` (BAAI embedding, 1024 размерности, multilingual) через Ollama
- **LLmWare.py**: встроенный `mini-lm-sbert` быстрый, но для лучшего качества RU/KZ лучше менять на `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`

Для смены модели в LLmWare.py:
```python
EMBEDDING_MODEL = "sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2"
```

## 📄 Входящие документы

Положите файлы в папку **`doc/`** (создастся автоматически):
- `.pdf` файлы
- `.docx` файлы
- `.txt` файлы (для LLmWare)

Оба скрипта **автоматически скануют папку** и индексируют только новые/изменённые файлы.

### Если папка `doc/` пустая
- Индекс и кэш **автоматически удаляются** (clean slate)
- При добавлении файлов индексация начнётся с нуля


## 🏗️ Архитектура данных

### LangG.py (LangChain + Qdrant)
```
doc/ (входящие PDF/DOCX)
    ↓
Docling (парсинг → Markdown)
    ↓
MarkdownHeaderTextSplitter + RecursiveCharacterTextSplitter (чанкинг)
    ↓
OllamaEmbeddings (bge-m3, 1024 dims) → создание векторов
    ↓
QdrantVectorStore (on-disk, qdrant_data/) → хранение
    ↓
Query (semantic search) → поиск релевантных чанков
    ↓
ChatOllama (qwen2.5:7b) → генерация ответа
```

### LLmWare.py (llmware + ChromaDB)
```
doc/ (входящие PDF/DOCX)
    ↓
llmware.Library.add_files() (встроенный парсинг)
    ↓
автоматический чанкинг (встроенный)
    ↓
sentence-transformers (mini-lm-sbert или multilingual MiniLM) → векторизация
    ↓
ChromaDB (внутри llmware_data/) → хранение
    ↓
Query.semantic_query() → поиск
    ↓
ChatOllama (qwen2.5:7b) → генерация ответа
```
